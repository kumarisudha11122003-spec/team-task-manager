# Set to the backend's public Railway domain during build.
# This is baked into the JS bundle at build time by Create React App.
# Example: https://team-task-manager-production.up.railway.app/api
# Leave unset (or omit this file) for local development — the CRA proxy handles it.
REACT_APP_API_URL=https://your-backend.railway.app/api
